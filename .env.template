# ============================================================================
# Template de variables de entorno - UnionX
# ============================================================================
# IMPORTANTE: las variables sensibles (passwords) NO se ponen aqui.
# Se configuran como variables de entorno del USUARIO Windows:
#   [Environment]::SetEnvironmentVariable("NOMBRE_VAR", "valor", "User")
#
# Las variables NO sensibles (paths, ids publicos) si pueden ir en .env real.

# Google Credentials (path al credentials.json - usar GOOGLE_APPLICATION_CREDENTIALS)
GOOGLE_APPLICATION_CREDENTIALS=agente-comex/config/credentials.json

# Email de Andres (para descargar EERR que envia Victor)
ANDRES_EMAIL=andres@unionx.cl

# === PASSWORDS (NO poner aqui - usar Windows env vars) ===
# ANDRES_ODOO_PASSWORD  -> setear con SetEnvironmentVariable("ANDRES_ODOO_PASSWORD", "...", "User")
# ANDRES_PASSWORD (Gmail App Password para IMAP) -> idem

# Rutas (resueltas automaticamente por el codigo, no hace falta hardcodear)
# CONTRIBUCION_FILE: el codigo la busca en data/planillas/, eerr-finanzas/ o ~/Desktop/Junior Revenue/

# DB del sincronizador Odoo (opcional - default: data/db/maestra_ventas.db)
# MAESTRA_VENTAS_DB=g:/Mi unidad/TRABAJO/RESPALDO/OPERACIONES/UNION X - IA/data/db/maestra_ventas.db
