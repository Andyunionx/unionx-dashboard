# Dependencias del agente de cobranza
# (instalar con: pip install -r agente-cobranza/requirements.txt)

# Config YAML
PyYAML>=6.0

# Excel
openpyxl>=3.1.0

# Google Drive API
google-api-python-client>=2.100.0
google-auth>=2.20.0
google-auth-httplib2>=0.1.0

# OdooClient ya vive en finanzas-unionx/backend/ — no se duplica acá
