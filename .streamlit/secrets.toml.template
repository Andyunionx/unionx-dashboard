# SECRETS TEMPLATE — copiar contenido a Streamlit Cloud:
# Streamlit Cloud → tu app → Settings → Secrets → Edit
#
# Para uso LOCAL: copiar a .streamlit/secrets.toml (gitignored)

# =================== Turso (libSQL cloud DB) ===================
LIBSQL_URL = "libsql://unionx-ventas-TUUSUARIO.turso.io"
LIBSQL_AUTH_TOKEN = "eyJhbGciOiJ..."

# =================== Odoo (para skill de extracción) ===================
ANDRES_ODOO_PASSWORD = "tu_password_odoo"

# =================== Auth multi-usuario ===================
[auth.credentials.usernames.andres]
email = "andres@unionx.cl"
name = "Andrés Browne"
# Hash bcrypt — generar con: python _generar_auth_local.py email pass nombre
password = "$2b$12$..."

# Agregar más usuarios:
# [auth.credentials.usernames.usuario2]
# email = "persona@unionx.cl"
# name = "Nombre Persona"
# password = "$2b$12$..."

[auth.cookie]
expiry_days = 7
key = "unionx-cookie-secret-CAMBIAR"
name = "unionx_dashboard_auth"

[auth.preauthorized]
emails = ["andres@unionx.cl"]
